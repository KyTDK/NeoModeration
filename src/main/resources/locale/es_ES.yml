error:
  no-permission: "&cNo tienes permiso para usar NeoModeration."
help:
  title: "&bNeoModeration"
  setup: "&e/{label} setup <apiKey> &7— activar moderación en la nube"
  on-off: "&7/{label} on|off"
  key: "&7/{label} key <apiKey>|clear"
  word: "&7/{label} word add|remove|list [palabra]"
  url: "&7/{label} url add|remove|list [enlace]"
  status: "&7/{label} status"
  reload: "&7/{label} reload"
setup:
  usage: "&cUso: /{label} setup <apiKey>"
  done: "&aModeración en la nube activada. El chat se está escaneando."
toggle:
  on: "&aModeración ACTIVADA."
  off: "&eModeración DESACTIVADA."
key:
  usage: "&cUso: /{label} key <apiKey> o /{label} key clear"
  saved: "&aClave API guardada."
  cleared: "&eClave API eliminada. Las reglas locales siguen activas."
status:
  title: "&bNeoModeration"
  enabled: "&7Estado: &f{value}"
  cloud: "&7Nube: &f{value}"
  rules: "&7Reglas locales: &f{words} palabras, {urls} enlaces"
reload:
  done: "&aRecargado."
rules:
  usage: "&cUso: /{label} {kind} add|remove|list [valor]"
  list: "&7{kind}s bloqueados: &f{values}"
  value-required: "&cAñade una palabra o enlace."
  updated: "&a{action} {kind}: &f{value}"
  unchanged: "&eSin cambios para {kind}: &f{value}"
